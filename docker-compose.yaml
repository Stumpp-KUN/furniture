version: '2'
services:

  postgres:
    image: postgres:16.0
    hostname: 6ebf8cc8f5d6
    environment:
      POSTGRES_USER: root
      POSTGRES_DB: furniture
      POSTGRES_PASSWORD: root
    ports:
      - "5332:5432"
    volumes:
      - /var/lib/postgresql/data